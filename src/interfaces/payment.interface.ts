export interface Payload {
  card_number: string;
  cvv: string;
  expiry_month: string;
  expiry_year: string;
  currency: string;
  amount: string;
  redirect_url: string;
  fullname: string;
  email: string;
  phone_number: string;
  enckey: string;
  tx_ref: string;
}
